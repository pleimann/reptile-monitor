# docker run --rm -v "${PWD}":/config -it esphome/esphome wesley.yaml compile
# docker run --rm -v "${PWD}":/config -it esphome/esphome winnie.yaml compile
docker run --rm -v "${PWD}":/config -it esphome/esphome waffle.yaml compile